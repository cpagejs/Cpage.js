import targetDom from './targetDom';
/**
 * 动画操作
 */
export default class moveModel extends targetDom {
    constructor(selector: any);
    show(delay: number): this;
    hide(delay: number): this;
}
